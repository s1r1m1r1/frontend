import 'package:flutter/material.dart';

class AppStageDialog extends StatelessWidget {
  const AppStageDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('loading'));
  }
}
