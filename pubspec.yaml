name: frontend
description: "A Flutter project for the frontend of the application."
publish_to: "none"
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.8.1 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get_it: ^8.0.3
  injectable: ^2.5.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  go_router: ^16.0.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  # bloc: ^9.0.0
  # flutter_bloc: ^9.1.1
  logging: ^1.3.0
  flutter_web_plugins:
    sdk: flutter
  meta: ^1.16.0
  equatable: ^2.0.7
  drift: ^2.14.0 # Or the latest version
  sqlite3_flutter_libs: ^0.5.0 # Required for Flutter
  path_provider: ^2.1.3 # For getting a path to store the database
  path: ^1.9.0 # For path manipulation
  drift_dev: ^2.14.0
  rxdart: ^0.27.7
  sha_red:
    git: https://github.com/s1r1m1r1/sha_red.git
  shared_preferences: ^2.5.3
  web_socket_channel: ^3.0.3
  web_socket_client: ^0.2.1
  # bloc_concurrency: ^0.3.0
  listen_it: ^5.0.0
  chopper: ^8.3.0
  http: ^1.5.0
  event_bus: ^2.0.1
  provider: ^6.1.5+1

# dependency_overrides:
#   sha_red:
#     path: ../sha_red

dev_dependencies:
  build_runner: ^2.5.0
  injectable_generator: ^2.5.0
  json_serializable: ^6.9.0
  freezed: ^3.1.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  go_router_builder: ^3.0.0
  chopper_generator:

flutter:
  uses-material-design: true # for icons
  generate: true # <--- l10n gen
